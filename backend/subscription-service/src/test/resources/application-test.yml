spring:
  flyway:
    create-schemas: true
    schemas: subscription
  jpa:
    hibernate:
      ddl-auto: validate

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com.farm2home.subscription: DEBUG
