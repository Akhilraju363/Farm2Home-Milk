package com.farm2home.common.core.constants;

/** Reserved for cache key prefixes / TTLs once a caching layer (e.g. Redis) is
 *  introduced - no service uses a cache today, so there is nothing to centralize yet.
 *  Kept as an empty placeholder so future cache constants have an established home
 *  rather than being scattered ad hoc when that work starts. */
public final class CacheConstants {

    private CacheConstants() {
    }
}
