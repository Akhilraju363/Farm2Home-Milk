spring:
  flyway:
    create-schemas: true
    schemas: delivery
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: delivery

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com.farm2home.delivery: DEBUG
