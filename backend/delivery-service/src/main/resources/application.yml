server:
  port: 8086

spring:
  application:
    name: delivery-service
  datasource:
    url: jdbc:postgresql://localhost:5432/farm2home?currentSchema=delivery
    username: farm2home
    password: farm2home@123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: delivery
  flyway:
    enabled: true
    schemas: delivery
    locations: classpath:db/migration

jwt:
  secret: ${JWT_SECRET:farm2home-secret-key-change-in-production-min-256-bits}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.farm2home.delivery: DEBUG
