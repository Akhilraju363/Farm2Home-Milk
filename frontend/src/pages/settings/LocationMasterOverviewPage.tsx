import { Box, Card, CardActionArea, CardContent, Grid, Typography } from '@mui/material'
import { AccountTree, ArrowForward, LocationCity, Map, Public } from '@mui/icons-material'
import { useNavigate } from 'react-router-dom'
import { PageHeader } from '../../components/common/PageHeader'
const cards = [{ label: 'States', text: 'Manage Indian states and union territories.', icon: <Public color="primary" />, path: 'states' }, { label: 'Districts', text: 'Manage districts within each state.', icon: <Map color="primary" />, path: 'districts' }, { label: 'Cities', text: 'Manage cities within each district.', icon: <LocationCity color="primary" />, path: 'cities' }]
export function LocationMasterOverviewPage() { const navigate = useNavigate(); return <Box><PageHeader title="Location Master" subtitle="Maintain the State → District → City hierarchy used for delivery addresses." /><Grid container spacing={2.5}>{cards.map((c) => <Grid item xs={12} md={4} key={c.path}><Card variant="outlined" sx={{ borderRadius: 2, height: '100%' }}><CardActionArea onClick={() => navigate(`/settings/location-master/${c.path}`)}><CardContent sx={{ minHeight: 165 }}>{c.icon}<Typography variant="h6" fontWeight={700} mt={1.5}>{c.label}</Typography><Typography variant="body2" color="text.secondary" mt={1}>{c.text}</Typography><Typography variant="body2" color="primary.main" fontWeight={700} mt={2}>Manage <ArrowForward sx={{ fontSize: 16, verticalAlign: 'text-bottom' }} /></Typography></CardContent></CardActionArea></Card></Grid>)}</Grid><Box sx={{ display: 'flex', alignItems: 'center', gap: 1, mt: 3, color: 'text.secondary' }}><AccountTree /><Typography variant="body2">Changes are reflected in the address selection flow.</Typography></Box></Box> }
