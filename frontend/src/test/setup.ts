// Vitest global setup — referenced by `test.setupFiles` in vite.config.ts.
// Runs once before the test suite. Add global test configuration here
// (e.g. @testing-library/jest-dom matchers) as the suite grows.
export {}
